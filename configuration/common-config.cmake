add_definitions(-D_USE_MATH_DEFINES)

IF(BUILD_K52_LIBRARY_TYPE STREQUAL Static)
    SET(BUILD_K52_STATIC TRUE)
    SET(BUILD_K52_DYNAMIC FALSE)
ELSE()
    IF(BUILD_K52_LIBRARY_TYPE STREQUAL Dynamic)
        SET(BUILD_K52_STATIC FALSE)
        SET(BUILD_K52_DYNAMIC TRUE)
    ELSE()
        IF(BUILD_K52_LIBRARY_TYPE STREQUAL DynamicAndStatic)
            SET(BUILD_K52_STATIC TRUE)
            SET(BUILD_K52_DYNAMIC TRUE)
        ELSE()
        #Default if Static
            SET(BUILD_K52_STATIC TRUE)
            SET(BUILD_K52_DYNAMIC FALSE)
        ENDIF()
    ENDIF()
ENDIF()
